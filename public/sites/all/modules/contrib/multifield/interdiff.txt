diff --git a/multifield.migrate.inc b/multifield.migrate.inc
index 7ce48a7..02dc089 100644
--- a/multifield.migrate.inc
+++ b/multifield.migrate.inc
@@ -54,16 +54,11 @@ class MultifieldMigrateFieldHandler extends MigrateFieldHandler {
   public function fields($type, $instance, $migration = NULL) {
     $output = array();
 
+    $handler = new MigrateFieldsEntityHandler();
     // Display the field label and name for each field in a multifield.
-    $fields = field_info_instances('multifield', $type);
-    foreach ($fields as $field_name => $field_info) {
-      $output[$field_name] = t('Subfield: @label (@field_name)', array(
-        '@label' => $field_info['label'],
-        '@field_name' => $field_name,
-      ));
-    }
+    $fields = call_user_func(array($handler, 'fields'), 'multifield', $type, $migration);
 
-    return $output;
+    return $fields;
   }
 
   /**
